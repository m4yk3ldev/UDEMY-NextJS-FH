const Navbar = () => {
  return (<>
    <div style={
      {
        display: 'flex',
        width: '100',
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'start',
        padding: "0 20px",
        backgroundColor: 'red'
      }
    }>
      <span>Navbar</span>
    </div>
  </>);
};

export default Navbar;
